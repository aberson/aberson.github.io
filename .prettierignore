# Dependencies & build output
node_modules/
dist/
.astro/
package-lock.json

# Vendored, generated brand tokens — keep VERBATIM (see header "DO NOT EDIT").
# Re-sync from ../aberson-profile/brand/dist/ rather than formatting in place.
src/assets/tokens.css
src/assets/theme.tw.css
src/assets/diagram-palette.json

# Docs with hand-aligned markdown tables Prettier would reformat on every run
# (README.md), plus workspace docs maintained outside the build pipeline
# (CLAUDE.md via /repo-update, plan.md). Kept off the Prettier gate to avoid churn.
CLAUDE.md
README.md
plan.md

# build-step orchestration scratch
.build-step/
